acl internal {
  localhost;
  localnets;
  192.168.100.0/22;
};

options {
  directory "/var/cache/bind";
  recursion no;
  allow-transfer { none; };
  allow-query { internal; };
  allow-recursion { internal; };
};
